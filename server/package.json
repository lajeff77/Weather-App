{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "description": "a weather app where you can look up the forecast for the day by name of city",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lauryn Jefferson",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.2"
  }
}
